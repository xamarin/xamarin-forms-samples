<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:common="using:XamlSamples.Common"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             x:Class="XamlSamples.Views.StaticConstantsPage"
             Title="Static Constants Page"
             Padding="{x:Static common:AppConstants.PagePadding}">

    <StackLayout>
        <Label Text="Hello, XAML!"
               TextColor="{x:Static common:AppConstants.BackgroundColor}"
               BackgroundColor="{x:Static common:AppConstants.ForegroundColor}"
               Font="{x:Static common:AppConstants.TitleFont}"
               HorizontalOptions="Center" />

        <BoxView WidthRequest="{x:Static sys:Math.PI}"
                 HeightRequest="{x:Static sys:Math.E}"
                 Color="{x:Static common:AppConstants.ForegroundColor}"
                 HorizontalOptions="Center"
                 VerticalOptions="CenterAndExpand"
                 Scale="100" />
    </StackLayout>
</ContentPage>